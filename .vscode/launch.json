{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run in terminal",
            "type": "PowerShell",
            "request": "launch",
            "script": "../run.ps1",
            "cwd": "${workspaceFolder}/bin",
            "preLaunchTask": "Build"
        }
    ]
}